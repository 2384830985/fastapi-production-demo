# Web 框架：基于 Starlette + Pydantic，支持异步、自动生成 OpenAPI 文档
fastapi>=0.115.0

# ASGI 服务器：用于运行 FastAPI 应用，[standard] 包含 uvloop/httptools 等加速依赖
uvicorn[standard]>=0.32.0

# WSGI 服务器：生产用 gunicorn 管理 多个 uvicorn worker，支持平滑重启/信号处理
gunicorn>=23.0.0

# 数据校验库：FastAPI 用它做请求/响应模型校验，v2 版本性能更好
pydantic>=2.9.0

# 密码哈希算法库：直接用官方 bcrypt 包，比 passlib 更轻量且无兼容性警告
# - gensalt(rounds=12) 生成盐值
# - hashpw/checkpw 哈希与校验
bcrypt>=4.0.0

# ORM 框架：2.0+ 推荐用 Mapped/mapped_column 类型注解写法
SQLAlchemy>=2.0.0

# 数据库迁移工具：管理 schema 变更，支持 autogenerate
alembic>=1.13.0

# MySQL 驱动：纯 Python 实现，兼容 MySQL 9.x 的 caching_sha2_password 认证
PyMySQL>=1.1.0

# .env 文件加载：把 .env 中的键值对加载到 os.environ，避免敏感配置硬编码
python-dotenv>=1.0.0

# JWT 编解码：签发/校验 access token，HS256 算法即可
PyJWT>=2.8.0

# 限流：防止暴力破解和 DDoS，登录接口尤其需要
slowapi>=0.1.9
