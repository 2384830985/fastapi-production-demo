# 开发依赖：只在本地开发/测试时需要，不进生产镜像
# 安装：pip install -r requirements-dev.txt

# 测试框架：比 unittest 更好用，支持 fixture/参数化
pytest>=8.0.0

# 测试覆盖率：生成覆盖率报告
pytest-cov>=5.0.0

# FastAPI 测试客户端依赖
httpx>=0.27.0

# 代码风格检查
flake8>=7.0.0

# 类型检查
mypy>=1.10.0

# 依赖锁文件工具（生成 requirements.lock）
pip-tools>=7.4.0

# Git pre-commit hook 工具
pre-commit>=3.7.0
